{% extends 'base.html.twig' %}
{% block body %}
<table>
    <tr>
        <td>nom</td>
        <td>{{ user.name }}</td>
    </tr>
    <tr>
        <td>email</td>
        <td>{{ user.email }}</td>
    </tr>
    <tr>
        <td>country</td>
        <td>{{ user.country }}</td>
    </tr>
    <tr>
        <td>date d'inscription</td>
        <td>{{ user.registerDate|date('Y-m-d H:i:s') }}</td>
    </tr>
</table>

<h2>Series suivies</h2>

<ul>

{% for series in user.series %}
    <li>{{series.title}}</li>
{% endfor %}
</ul>

{% endblock %}